//import { fibonacciTool, homeNavTools, moreLineTool, polyTool, textTool, waveTool } from "@/app/commands/homeNavTools";

export enum CommandPosition {
  Header = "Header",
  Nav = "Nav",
  Footer = "Footer",
  Docker = "Docker",
  ContentMenu = "ContentMenu",
  Sidebar = "Sidebar",
  Main = "Main",
  Background = "Background",
  Anonymous = "Anonymous"
}
